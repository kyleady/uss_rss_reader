<%#Header of the page%>
<h1>Feeds#new</h1>
<%= form_for(@feed) do |f| %>
  <ul>
  <% @feed.errors.full_messages.each do |error| %>
    <li><%= error %></li>
  <% end %>
  <% if flash.alert %>
    <li><%= flash.alert %></li>
  <% end %>
  </ul>
  <p>
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to "<< Back to Feeds List", feeds_path %>
